{
  "name": "lambdas",
  "private": true,
  "workspaces": [
    "functions/*",
    "libs/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "dist": "yarn workspaces run build && yarn workspaces run dist",
    "format": "yarn workspaces run format",
    "format-check": "yarn workspaces run format-check",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test"
  },
  "resolutions": {
    "@octokit/types": "^12.0.0"
  },
  "devDependencies": {
    "@nx/eslint": "18.0.4",
    "@nx/jest": "18.0.4",
    "@nx/js": "^18.0.4",
    "@swc-node/register": "~1.8.0",
    "@swc/core": "~1.3.85",
    "@swc/helpers": "~0.5.2",
    "jest": "^29.4.1",
    "nx": "18.0.4",
    "prettier": "^2.6.2"
  },
  "dependencies": {}
}
